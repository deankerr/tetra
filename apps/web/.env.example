VITE_SYNC_WORKER_URL=ws://localhost:8787

# Dev-only seeding (ignored in production builds): fills blank values on boot, never
# overwrites what was set in the UI. Useful for agents with fresh browser environments.
# VITE_OPENROUTER_API_KEY=sk-or-v1-...
# VITE_SYNC_KEY=...
