# Web: memory | local | sync
VITE_TETRA_DATA_MODE=memory
VITE_WORKER_URL=ws://localhost:8787

# CLI sync, dump, and reset commands
TETRA_WORKER_URL=ws://localhost:8787

# Worker and CLI synced-store reset. Set the real remote value with:
# bunx wrangler secret put TETRA_RESET_TOKEN
# TETRA_RESET_TOKEN=change-me
